<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="Groundsman.EditFeatureDetailsView"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
             x:Name="EditFeatureDetailsViewPage"
             ios:Page.ModalPresentationStyle="FormSheet"
             BackgroundColor="{DynamicResource bgPrimary}">
    <ContentPage.ToolbarItems>
        <ToolbarItem Command="{Binding OnSaveUpdatedCommand}" Icon="save_icon" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout Margin="10,10,10,30">
            <Frame Style="{StaticResource FrameStyle}" VerticalOptions="Start">
                <StackLayout>
                    <Grid ColumnSpacing="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.6*" />
                            <ColumnDefinition Width="0.4*" />
                        </Grid.ColumnDefinitions>
                        <Label Grid.Row="0"
                               Grid.Column="0"
                               Style="{StaticResource ViewLabelStyle}"
                               Text="Name" />
                        <Entry x:Name="nameEntry"
                               Grid.Row="1"
                               Grid.Column="0"
                               AutomationId="nameEntry"
                               FontSize="Small"
                               Placeholder="Name of item"
                               Style="{StaticResource EntryStyle}"
                               Text="{Binding NameEntry}" />
                        <Label Grid.Row="0"
                               Grid.Column="1"
                               Style="{StaticResource ViewLabelStyle}"
                               Text="Date" />
                        <Label x:Name="dateEntry"
                               Grid.Row="1"
                               Grid.Column="1"
                               Margin="0,8,0,0"
                               AutomationId="dateEntry"
                               FontSize="Small"
                               Text="{Binding DateEntry, StringFormat='{0:dd/MM/yyyy}'}"
                               TextColor="Gray" />
                    </Grid>
                </StackLayout>
            </Frame>
            <Frame Style="{StaticResource FrameStyle}" VerticalOptions="Start">
                <StackLayout>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Label Grid.Row="0"
                               Grid.Column="0"
                               Style="{StaticResource ViewLabelStyle}"
                               Text="Geolocation Data" />
                        <Button x:Name="addPointBtn"
                                Grid.Row="0"
                                Grid.Column="1"
                                AutomationId="addGeoPoint"
                                Command="{Binding AddPointCommand}"
                                FontSize="Small"
                                HeightRequest="28"
                                HorizontalOptions="End"
                                WidthRequest="32" />
                    </Grid>
                    <ListView x:Name="geolocationListView"
                              AutomationId="geolocationListView"
                              HasUnevenRows="True"
                              ItemTapped="listView_ItemTapped"
                              ItemsSource="{Binding GeolocationValues}"
                              SeparatorColor="Gray"
                              Style="{StaticResource ListViewStyle}"
                              VerticalOptions="Start">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ViewCell>
                                    <Grid Margin="10,0,0,0"
                                          IsEnabled="{Binding Path=BindingContext.GeolocationEntryEnabled, Source={x:Reference Name=EditFeatureDetailsViewPage}}"
                                          Style="{StaticResource DetailFormPanelGrid}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition />
                                            <ColumnDefinition />
                                            <ColumnDefinition />
                                            <ColumnDefinition Width="32" />
                                        </Grid.ColumnDefinitions>
                                        <Label Grid.Row="0"
                                               Grid.Column="0"
                                               Margin="0,9"
                                               AutomationId="LatLabel"
                                               FontSize="Small"
                                               Style="{StaticResource ViewLabelStyle}"
                                               Text="Latitude" />
                                        <Entry Grid.Row="1"
                                               Grid.Column="0"
                                               Margin="0,0,0,10"
                                               AutomationId="LatitudeEntry"
                                               FontSize="Small"
                                               Keyboard="Numeric"
                                               Style="{StaticResource EntryStyle}"
                                               Text="{Binding Latitude}" />
                                        <Label Grid.Row="0"
                                               Grid.Column="1"
                                               Margin="0,9"
                                               AutomationId="LongLabel"
                                               FontSize="Small"
                                               Style="{StaticResource ViewLabelStyle}"
                                               Text="Longitude" />
                                        <Entry Grid.Row="1"
                                               Grid.Column="1"
                                               Margin="0,0,0,10"
                                               AutomationId="LongitudeEntry"
                                               FontSize="Small"
                                               Keyboard="Numeric"
                                               Style="{StaticResource EntryStyle}"
                                               Text="{Binding Longitude}" />
                                        <Label Grid.Row="0"
                                               Grid.Column="2"
                                               Margin="0,9"
                                               AutomationId="AltLabel"
                                               FontSize="Small"
                                               Style="{StaticResource ViewLabelStyle}"
                                               Text="Altitude" />
                                        <Entry Grid.Row="1"
                                               Grid.Column="2"
                                               Margin="0,0,0,10"
                                               AutomationId="AltitudeEntry"
                                               FontSize="Small"
                                               Keyboard="Numeric"
                                               Style="{StaticResource EntryStyle}"
                                               Text="{Binding Altitude}" />
                                        <Button Grid.Row="0"
                                                Grid.Column="3"
                                                Command="{Binding Path=BindingContext.DeletePointCommand, Source={x:Reference Name=EditFeatureDetailsViewPage}}"
                                                CommandParameter="{Binding .}"
                                                FontSize="Small"
                                                HeightRequest="20"
                                                Image="delete_icon"
                                                IsEnabled="{Binding Path=BindingContext.GeolocationEntryEnabled, Source={x:Reference Name=EditFeatureDetailsViewPage}}"
                                                IsVisible="{Binding Path=BindingContext.ShowPointDeleteBtn, Source={x:Reference Name=EditFeatureDetailsViewPage}}"
                                                WidthRequest="20" />
                                        <ActivityIndicator Grid.Row="1"
                                                           Grid.Column="3"
                                                           IsRunning="{Binding Path=BindingContext.LoadingIconActive, Source={x:Reference Name=EditFeatureDetailsViewPage}}"
                                                           IsVisible="{Binding Path=BindingContext.LoadingIconActive, Source={x:Reference Name=EditFeatureDetailsViewPage}}"
                                                           Color="{StaticResource greenPrimary}" />
                                        <Button Grid.Row="1"
                                                Grid.Column="3"
                                                Margin="0,0,0,10"
                                                Command="{Binding Path=BindingContext.GetFeatureCommand, Source={x:Reference Name=EditFeatureDetailsViewPage}}"
                                                CommandParameter="{Binding .}"
                                                HeightRequest="20"
                                                Image="current_location"
                                                IsEnabled="{Binding Path=BindingContext.GeolocationEntryEnabled, Source={x:Reference Name=EditFeatureDetailsViewPage}}"
                                                WidthRequest="20" />
                                    </Grid>
                                </ViewCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.Footer>
                            <Button x:Name="closePolyBtn"
                                    Command="{Binding ClosePolyCommand}"
                                    Text="Close Polygon">
                                <Button.TextColor>
                                    <OnPlatform x:TypeArguments="Color"
                                                Android="White"
                                                iOS="{StaticResource greenPrimary}" />
                                </Button.TextColor>
                            </Button>
                        </ListView.Footer>
                    </ListView>
                </StackLayout>
            </Frame>
            <Frame HeightRequest="200"
                   Style="{StaticResource FrameStyle}"
                   VerticalOptions="Start">
                <StackLayout>
                    <Label Style="{StaticResource ViewLabelStyle}" Text="Metadata" />
                    <StackLayout Orientation="Horizontal">
                        <Label FontSize="Small"
                               HorizontalOptions="Start"
                               MinimumWidthRequest="120"
                               Style="{StaticResource ViewLabelStyle}"
                               Text="String:"
                               VerticalTextAlignment="Center" />
                        <Entry FontSize="Small"
                               HorizontalOptions="FillAndExpand"
                               Keyboard="Default"
                               Placeholder="Enter string..."
                               Text="{Binding MetadataStringEntry}" />
                    </StackLayout>
                    <StackLayout Orientation="Horizontal">
                        <Label FontSize="Small"
                               MinimumWidthRequest="120"
                               Style="{StaticResource ViewLabelStyle}"
                               Text="Integer:"
                               VerticalTextAlignment="Center" />
                        <Entry FontSize="Small"
                               HorizontalOptions="FillAndExpand"
                               Keyboard="Numeric"
                               Text="{Binding MetadataIntegerEntry}" />
                    </StackLayout>
                    <StackLayout Orientation="Horizontal">
                        <Label FontSize="Small"
                               MinimumWidthRequest="120"
                               Style="{StaticResource ViewLabelStyle}"
                               Text="Float:"
                               VerticalTextAlignment="Center" />
                        <Entry FontSize="Small"
                               HorizontalOptions="FillAndExpand"
                               Keyboard="Numeric"
                               Text="{Binding MetadataFloatEntry}" />
                    </StackLayout>
                </StackLayout>
            </Frame>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>